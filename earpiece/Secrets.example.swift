import Foundation

/// TEMPLATE — copy this file to `Secrets.swift` (same folder) and paste your own
/// OpenAI API key. `Secrets.swift` is git-ignored so your real key never leaves
/// your machine; this placeholder version is the one that's safe to commit.
///
/// ▸ Get a key at https://platform.openai.com/api-keys (it starts with "sk-...").
/// ▸ Paste it in Xcode — never into a chat, a commit, or a screenshot.
enum Secrets {
    static let openAIKey = "PASTE_YOUR_OPENAI_KEY_HERE"
}
